let numeros = [10, 5, 8, 20, 15];
let soma = 0;
for (let i = 0; i < numeros.length; i++) {
  soma += numeros[i];
}

console.log("A soma dos números é " + soma);
console.log("A media do array é " + soma / numeros.length);
